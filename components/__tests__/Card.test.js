import React from 'react'
import { render, screen, fireEvent, act, waitFor } from '@testing-library/react-native'

describe('Card component', () => {
    it('it should say HELD when a card is pressed', () => {

    })
})